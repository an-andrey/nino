[project]
name = "nino"
version = "1.0.0"
description = "Default template for PDM package"
authors = [
    {name = "", email = ""},
]
dependencies = ["torch-geometric>=2.8.0.post1", "transformers>=5.14.1", "datasets>=5.0.0", "torch>=2.13.0"]
requires-python = "==3.11.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
